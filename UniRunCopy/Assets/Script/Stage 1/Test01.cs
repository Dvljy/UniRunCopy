using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Test01 : MonoBehaviour
{

    void Start()
    {
        /*int a = 80;
        int b = 43;
        int c = 16;
        int sum;
        int avg;
        Debug.Log($"합계는 {sum}이고 평균은 {avg}이다");
        
        int sum = 0;

        for (int i = 0; i <= 10; i++)
        {
            if (i % 3 != 0)
            {
                Debug.Log(i);
                sum += i;
            }
        }
        Debug.Log($"1~10중 3으로 나누어 떨어지지 않는 수의 합 : {sum}");


        int[] sc = new int[] { 10, 20, 30, 40, 50 };
        int sum = 0;

        foreach (int a in sc)
        {
            sum += a;
        }
        Debug.Log($"{sc.Length}명 학생의 평균 : {sum / sc.Length}");
        */
    }


    void Update()
    {

    }
}
